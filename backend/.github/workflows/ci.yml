# !! THIS FILE IS NOT EXECUTED BY GITHUB ACTIONS !!
#
# GitHub Actions only reads workflow files from:
#   /<repo-root>/.github/workflows/
#
# This file was previously located here (backend/.github/workflows/ci.yml)
# which is why CI was silently broken — no tests ever ran on any push or PR.
#
# The workflow has been moved to the correct location:
#   /.github/workflows/ci.yml          (backend tests, Docker build)
#   /.github/workflows/frontend-ci.yml (frontend lint + build)
#
# DO NOT add workflow files in this directory.
# Reference: https://docs.github.com/en/actions/writing-workflows/quickstart
